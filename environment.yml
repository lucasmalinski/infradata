name: infradata_env
channels:
  - conda-forge
channel_priority: strict
dependencies:
  - python=3.11
  - pandas
  - geopandas
  - requests
  - python-dotenv
  - camelot-py
  - pip
  - pip:
    - -r requirements.txt
    - -e .
